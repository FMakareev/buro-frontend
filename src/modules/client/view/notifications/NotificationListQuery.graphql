query($clientid: String){
  notificationlist(clientid: $clientid) {
    id
    status
    date
    client {
      id
      email
      role
    }
    bank {
      id
      email
      bankName
      role
    }
    bureau {
      id

    }
  }
}
