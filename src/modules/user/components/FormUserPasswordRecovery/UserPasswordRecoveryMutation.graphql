mutation($token: String!,$newPassword: String!, $confirmPassword: String) {
  recoverypass(token:$token, newPassword: $newPassword, confirmPassword: $confirmPassword){
    recoverypass{
      id
      email
    }
  }
}
