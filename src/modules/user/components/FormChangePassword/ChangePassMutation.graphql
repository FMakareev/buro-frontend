mutation($id:String, $password: String!, $newPassword:String!, $confirmPassword:String! ) {
  changepass(id:$id, password:$password, newPassword:$newPassword, confirmPassword:$confirmPassword) {
    changepass {
      id
    }
  }
}
