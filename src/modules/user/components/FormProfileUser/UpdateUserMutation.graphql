mutation(
  $id: String,
$firstName: String,
$lastName: String,
$patronymic: String,
$birthdate: String,
$gender: String,
$email: String,
$phone: String,
$password: String,
$confirmPassword: String,
$role: String,
) {
  updateuser(
    id: $id,
    firstName:$firstName,
    lastName:$lastName,
    patronymic:$patronymic,
    birthdate:$birthdate,
    gender:$gender,
    email :$email ,
    phone:$phone,
    password:$password,
    confirmPassword :$confirmPassword ,
    role :$role ,
  ){
    id
    firstName
    lastName
    patronymic
    birthdate
    gender
    email
    phone
    role
  }
}
